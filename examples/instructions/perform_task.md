# Perform Task Step

The user wants you to perform a task. You MUST use the `execute_task` tool to complete their request.

Look at the [User Input] section to find the original task.

Call the `execute_task` tool exactly once with:
- result: The result of executing the task (e.g., for calculations, provide the answer)

Do NOT respond with plain text - you MUST call the execute_task tool.
